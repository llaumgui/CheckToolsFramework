# Global configuration for all checktools
global_config:
    # Like --path-exclusion:
    # Directory name pattern extension (can use regular expression)
    path_exclusion: "#tests/Resources/testing_files/|vendor/#"

    # Like --output:
    #Junit XML ouput
    #output: ""

    # Like --filename-exclusion
    # File name pattern extension (can use regular expression)
    #filename_exclusion: ""

    # Like --noignore-vcs
    #no_ignore_vcs: ""

checker:
    - bom
    - json
    - yaml
